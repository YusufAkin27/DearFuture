import 'package:flutter/material.dart';

/// Login ve verify ekranları için koyu tema renkleri.
/// Tüm metinler beyaz (siyah metin yok).
class LoginColors {
  LoginColors._();
  static const Color background = Color(0xFF19232F);
  static const Color surface = Color(0xFF243240);
  static const Color border = Color(0xFF3D4F5F);
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textLightGray = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFFFFFFFF);
  static const Color primaryStart = Color(0xFF00ACC1);
  static const Color primaryEnd = Color(0xFF26C6DA);
}
